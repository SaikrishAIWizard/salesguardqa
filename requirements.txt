fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
sqlalchemy==2.0.36
python-multipart==0.0.19
python-dotenv==1.2.3

# Optional: only needed if OPENAI_API_KEY is set to enable the LLM
# semantic fallback. The app works fully without it.
# openai==1.58.1
